<Window x:Class="GameFromHome.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Game From Home" Width="930" Height="850" MinWidth="600" MinHeight="590" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}" FontFamily="Segoe UI" FontSize="14" WindowStartupLocation="CenterScreen" Icon="Assets/App.png">
 <Grid x:Name="RootGrid">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Padding="25,13">
   <DockPanel><StackPanel Orientation="Horizontal" DockPanel.Dock="Left"><Image Source="Assets/App.png" Width="40" Height="40" Margin="0,0,10,0"/><TextBlock Text="Game From Home" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center"/></StackPanel><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button x:Name="RefreshButton" Content="Refresh" Click="RefreshClick" Margin="0,0,8,0"/><Button x:Name="PreferencesButton" Content="Preferences" Click="PreferencesClick"/></StackPanel></DockPanel>
  </Border>
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
   <StackPanel Margin="30,24,30,20">
    <Border x:Name="PreferencesPanel" Visibility="Collapsed" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="10" Padding="17" Margin="0,0,0,20"><StackPanel><TextBlock Text="Keep it simple" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12"/><CheckBox x:Name="AutoExitCheck" Content="Close Game From Home 8 seconds after a successful result" Checked="AutoExitChanged" Unchecked="AutoExitChanged" Margin="0,0,0,12"/><TextBlock Text="Selections are remembered after you press Free up RAM. Discord is always protected. Codex is optional and closes last. The app runs only when you open it." Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,12"/><Button Content="Create one-click shortcut" HorizontalAlignment="Left" Click="ShortcutClick"/><TextBlock x:Name="PreferencesNote" Foreground="{DynamicResource MutedBrush}" Margin="0,10,0,0"/></StackPanel></Border>
    <Grid Margin="0,0,0,23"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="175"/></Grid.ColumnDefinitions><StackPanel><TextBlock x:Name="Eyebrow" Text="WORK WRAPPED. PLAY NEXT." FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,10"/><TextBlock x:Name="Headline" Text="Make room for play." FontSize="34" FontWeight="SemiBold" Margin="0,0,0,9"/><TextBlock x:Name="Subtitle" Text="Quit your everyday apps in one go." Foreground="{DynamicResource MutedBrush}"/></StackPanel><StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"><TextBlock x:Name="AvailableText" Text="— GiB" FontSize="35" FontWeight="SemiBold" HorizontalAlignment="Right"/><TextBlock x:Name="TotalText" Text="Reading memory…" Foreground="{DynamicResource MutedBrush}" FontSize="12" HorizontalAlignment="Right"/></StackPanel></Grid>
    <Grid Height="8" Margin="0,0,0,9"><Grid.ColumnDefinitions><ColumnDefinition x:Name="UsedColumn" Width="55*"/><ColumnDefinition x:Name="FreeColumn" Width="45*"/></Grid.ColumnDefinitions><Border Background="{DynamicResource AccentBrush}" CornerRadius="4,0,0,4"/><Border Grid.Column="1" Background="{DynamicResource LineBrush}" CornerRadius="0,4,4,0"/></Grid>
    <DockPanel Margin="0,0,0,24"><TextBlock x:Name="UsedText" Text="Reading system RAM" FontSize="12" Foreground="{DynamicResource MutedBrush}"/><TextBlock Text="Available RAM" DockPanel.Dock="Right" HorizontalAlignment="Right" FontSize="12" Foreground="{DynamicResource MutedBrush}"/></DockPanel>
    <TextBlock x:Name="Notice" Foreground="#FFBD83" Visibility="Collapsed" Margin="0,0,0,16"/>
    <DockPanel Margin="0,0,0,12"><TextBlock x:Name="ListTitle" Text="Using over 500 MB" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="RAM usage · highest first" DockPanel.Dock="Right" HorizontalAlignment="Right" Foreground="{DynamicResource MutedBrush}" FontSize="12"/></DockPanel>
    <TextBlock x:Name="EmptyText" Text="No closable apps are using over 500 MB." Visibility="Collapsed" Foreground="{DynamicResource MutedBrush}" Margin="0,20"/>
    <Border BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" Background="{DynamicResource PanelBrush}" CornerRadius="10"><ListBox x:Name="AppList" Height="340" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextBrush}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"><ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><ContentPresenter/></ControlTemplate></Setter.Value></Setter></Style></ListBox.ItemContainerStyle><ListBox.ItemTemplate><DataTemplate><Border BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,0,0,1" Padding="15,11" ToolTip="{Binding Details}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="44"/><ColumnDefinition Width="*"/><ColumnDefinition Width="105"/><ColumnDefinition Width="55"/></Grid.ColumnDefinitions><CheckBox IsChecked="{Binding Selected, Mode=TwoWay}" IsEnabled="{Binding CanSelect}" VerticalAlignment="Center" AutomationProperties.Name="{Binding AccessibleName}"/><Border Grid.Column="1" Width="31" Height="31" HorizontalAlignment="Left" CornerRadius="7" Background="{DynamicResource LineBrush}" VerticalAlignment="Center"><Grid><TextBlock Text="{Binding Initials}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/><Image Source="{Binding AppIcon}" Width="30" Height="30"/></Grid></Border><StackPanel Grid.Column="2" VerticalAlignment="Center"><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding Status}" Foreground="{Binding StatusBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,2,0,0"/></StackPanel><TextBlock Grid.Column="3" Text="{Binding MemoryText}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/><Button Grid.Column="4" Content="Show" Padding="6,4" MinHeight="29" FontSize="11" Click="ShowAppClick" ToolTip="Show this app if it has an open window"/></Grid></Border></DataTemplate></ListBox.ItemTemplate></ListBox></Border>
    <TextBlock x:Name="ProtectedText" Text="Discord is always protected. Codex is optional and closes last." Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,17,0,0"/>
    <TextBlock x:Name="MethodText" Text="Only apps using over 500 MB with a supported clean-exit method are shown. App processes are grouped before applying the threshold." Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,12,0,0"/>
   </StackPanel>
  </ScrollViewer>
  <Border Grid.Row="2" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="0,1,0,0" Padding="28,20"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel VerticalAlignment="Center" Margin="0,0,15,0"><TextBlock x:Name="Summary" Text="Finding apps…" FontWeight="SemiBold"/><TextBlock x:Name="SummaryNote" Text="Normal exits. No forced shutdowns." FontSize="12" Foreground="{DynamicResource MutedBrush}" Margin="0,4,0,0"/></StackPanel><Button x:Name="StopButton" Grid.Column="1" Content="Stop remaining" Visibility="Collapsed" Click="StopClick" Margin="0,0,10,0"/><Button x:Name="PrimaryButton" Grid.Column="2" Content="Free up RAM" Background="{DynamicResource AccentBrush}" Foreground="#2A211B" BorderBrush="{DynamicResource AccentBrush}" FontWeight="SemiBold" MinWidth="160" Padding="21,14" Click="PrimaryClick" IsEnabled="False"/></Grid></Border>
 </Grid>
</Window>
